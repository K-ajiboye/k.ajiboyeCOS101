use std::io::Write;
use std::io;

fn main() {
    println!("Welcome to cadbury PLC");
     println!("Enter username: ");
     println!("clue: first 4 letters of your company name", );
     let mut username = String::new();
     io::stdin().read_line(&mut username).expect("failed to read input");

     // password criteria 
     println!("Enter password: ");
     println!("must be between letters (a-z), numbers between (0-9), no uppercase letters, no chracters from [$#@]");
     println!("maximum length of password:8, minimum length of password:3 ");
     let mut password = String::new();
     io::stdin().read_line(&mut password).expect("Not a valid input");
     let _password:i32 = password.trim().parse().expect("Not a valid number");
 
     let username = "cadb";
     let password = 2024;
     if username == "cadb" && password == 2024 {
        println!("welcome! successful login, {}", username);
     } 
     else {
        println!("Invalid login. please check username or password.");
     }   
    
     println!("Welcome to champion PLC");
     println!("Enter username: ");
     println!("clue: first 4 letters of your company name", );
     let mut username = String::new();
     io::stdin().read_line(&mut username).expect("failed to read input");

     // password criteria 
     println!("Enter password: ");
     println!("must be between letters (a-z), numbers between (0-9), no uppercase letters, no chracters from [$#@]");
     println!("maximum length of password:8, minimum length of password:3 ");
     let mut password = String::new();
     io::stdin().read_line(&mut password).expect("Not a valid input");
     let _password:i32 = password.trim().parse().expect("Not a valid number");
 
     let username = "cham";
     let password = 2025;
     if username == "cham" && password == 2025 {
        println!("welcome! successful login, {}", username);
     } 
     else {
        println!("Invalid login. please check username or password.");
     }   

     println!("Welcome to Dangote sugar PLC");
     println!("Enter username: ");
     println!("clue: first 4 letters of your company name", );
     let mut username = String::new();
     io::stdin().read_line(&mut username).expect("failed to read input");

     // password criteria 
     println!("Enter password: ");
     println!("must be between letters (a-z), numbers between (0-9), no uppercase letters, no chracters from [$#@]");
     println!("maximum length of password:8, minimum length of password:3 ");
     let mut password = String::new();
     io::stdin().read_line(&mut password).expect("Not a valid input");
     let _password:i32 = password.trim().parse().expect("Not a valid number");
 
     let username = "dang";
     let password = 2026;
     if username == "dang" && password == 2026 {
        println!("welcome! successful login, {}", username);
     } 
     else {
        println!("Invalid login. please check username or password.");
     }   
    
    println!("Welcome to Flour_mills PLC");
     println!("Enter username: ");
     println!("clue: first 4 letters of your company name", );
     let mut username = String::new();
     io::stdin().read_line(&mut username).expect("failed to read input");

     // password criteria 
     println!("Enter password: ");
     println!("must be between letters (a-z), numbers between (0-9), no uppercase letters, no chracters from [$#@]");
     println!("maximum length of password:8, minimum length of password:3 ");
     let mut password = String::new();
     io::stdin().read_line(&mut password).expect("Not a valid input");
     let _password:i32 = password.trim().parse().expect("Not a valid number");
 
     let username = "flou";
     let password = 2027;
     if username == "flou" && password == 2027 {
        println!("welcome! successful login, {}", username);
     } 
     else {
        println!("Invalid login. please check username or password.");
     }   

     println!("Welcome to nestle PLC");
     println!("Enter username: ");
     println!("clue: first 4 letters of your company name", );
     let mut username = String::new();
     io::stdin().read_line(&mut username).expect("failed to read input");

     // password criteria 
     println!("Enter password: ");
     println!("must be between letters (a-z), numbers between (0-9), no uppercase letters, no chracters from [$#@]");
     println!("maximum length of password:8, minimum length of password:3 ");
     let mut password = String::new();
     io::stdin().read_line(&mut password).expect("Not a valid input");
     let _password:i32 = password.trim().parse().expect("Not a valid number");
 
     let username = "nest";
     let password = 2028;
     if username == "nest" && password == 2028 {
        println!("welcome! successful login, {}", username);
     } 
     else {
        println!("Invalid login. please check username or password.");
     }   

     println!("Welcome to unilever PLC");
     println!("Enter username: ");
     println!("clue: first 4 letters of your company name", );
     let mut username = String::new();
     io::stdin().read_line(&mut username).expect("failed to read input");

     // password criteria 
     println!("Enter password: ");
     println!("must be between letters (a-z), numbers between (0-9), no uppercase letters, no chracters from [$#@]");
     println!("maximum length of password:8, minimum length of password:3 ");
     let mut password = String::new();
     io::stdin().read_line(&mut password).expect("Not a valid input");
     let _password:i32 = password.trim().parse().expect("Not a valid number");
 
     let username = "unil";
     let password = 2029;
     if username == "unil" && password == 2029 {
        println!("welcome! successful login, {}", username);
     } 
     else {
        println!("Invalid login. please check username or password.");
     }   

     println!("Welcome to Honeywell PLC");
     println!("Enter username: ");
     println!("clue: first 4 letters of your company name", );
     let mut username = String::new();
     io::stdin().read_line(&mut username).expect("failed to read input");

     // password criteria 
     println!("Enter password: ");
     println!("must be between letters (a-z), numbers between (0-9), no uppercase letters, no chracters from [$#@]");
     println!("maximum length of password:8, minimum length of password:3 ");
     let mut password = String::new();
     io::stdin().read_line(&mut password).expect("Not a valid input");
     let _password:i32 = password.trim().parse().expect("Not a valid number");
 
     let username = "hone";
     let password = 2030;
     if username == "hone" && password == 2030 {
        println!("welcome! successful login, {}", username);
     } 
     else {
        println!("Invalid login. please check username or password.");
     }   

     println!("Welcome to nigerian_breweries PLC");
     println!("Enter username: ");
     println!("clue: first 4 letters of your company name", );
     let mut username = String::new();
     io::stdin().read_line(&mut username).expect("failed to read input");

     // password criteria 
     println!("Enter password: ");
     println!("must be between letters (a-z), numbers between (0-9), no uppercase letters, no chracters from [$#@]");
     println!("maximum length of password:8, minimum length of password:3 ");
     let mut password = String::new();
     io::stdin().read_line(&mut password).expect("Not a valid input");
     let _password:i32 = password.trim().parse().expect("Not a valid number");
 
     let username = "nige";
     let password = 2031;
     if username == "nige" && password == 2031 {
        println!("welcome! successful login, {}", username);
     } 
     else {
        println!("Invalid login. please check username or password.");
     }   
  let _company = vec!["\n1)Cadbury\n","2)Champion\n","3)Dangote Sugar\n","4)Flour Mills\n","5)Nestle\n","6)Unilever\n","7)Honeywell\n","8)Nigerian Breweries\n"];
  let _shares = vec!["15_000_000\n","25_000_000\n","18_000_000\n","32_000_000\n","8_000_000\n","37_000_000\n","34_000_000\n","30_000_000\n"];
  let _liabilities = vec!["5_500_000 \n","8_000_000 \n","10_000_000 \n","4_000_000","1_500_000 \n","11_000_000 \n","9_000_000 \n","12_000_000 \n"];
  let _date_founded = vec!["1965\n","1974\n","1970\n","1960\n","1961\n","1923\n","1906\n","1946\n"];
  let _leverage = vec!["63.3%\n","68.0%\n","44.4%\n","87.5%\n","81.3%\n","70.3%\n","73.5%\n","60.0%\n"];
 
  let mut file = std::fs::File::create("ca2_project.txt").expect("create failed");

  file.write_all("\nThe information below displays companies and their assets.".as_bytes()).expect("write failed");
  
  let company = "company - Cadbury\n";
  let shares = "shares - 15_000_000\n";
  let liabilities = "liabilities - 5_500_000\n";
  let date_founded = "date_founded - 1965\n";
  let leverage = "leverage - 63.3%\n";

  let mut file = std::fs::File::create("cadbury.txt").expect("create failed");
  file.write_all(company.as_bytes()).expect("write failed");
  file.write_all(shares.as_bytes()).expect("write failed");
  file.write_all(liabilities.as_bytes()).expect("write failed");
  file.write_all(date_founded.as_bytes()).expect("write failed");
  file.write_all(leverage.as_bytes()).expect("write failed");

  let company = "company - Champion\n";
  let shares = "shares - 25_000_000\n";
  let liabilities = "liabilities - 8_000_000\n";
  let date_founded = "date_founded - 1974\n";
  let leverage = "leverage - 68.0%\n";

  let mut file = std::fs::File::create("champion.txt").expect("create failed");
  file.write_all(company.as_bytes()).expect("write failed");
  file.write_all(shares.as_bytes()).expect("write failed");
  file.write_all(liabilities.as_bytes()).expect("write failed");
  file.write_all(date_founded.as_bytes()).expect("write failed");
  file.write_all(leverage.as_bytes()).expect("write failed");

  let company = "company - Dangote Sugar\n";
  let shares = "shares - 18_000_000\n";
  let liabilities = "liabilities - 10_000_000\n";
  let date_founded = "date_founded - 1970\n";
  let leverage = "leverage - 44.4%\n";

  let mut file = std::fs::File::create("dangote_sugar.txt").expect("create failed");
  file.write_all(company.as_bytes()).expect("write failed");
  file.write_all(shares.as_bytes()).expect("write failed");
  file.write_all(liabilities.as_bytes()).expect("write failed");
  file.write_all(date_founded.as_bytes()).expect("write failed");
  file.write_all(leverage.as_bytes()).expect("write failed");

  let company = "company - Flour Mills\n";
  let shares = "shares - 32_000_000\n";
  let liabilities = "liabilities - 4_000_000\n";
  let date_founded = "date_founded - 1960\n";
  let leverage = "leverage - 87.5%\n";

  let mut file = std::fs::File::create("flour_mills.txt").expect("create failed");
  file.write_all(company.as_bytes()).expect("write failed");
  file.write_all(shares.as_bytes()).expect("write failed");
  file.write_all(liabilities.as_bytes()).expect("write failed");
  file.write_all(date_founded.as_bytes()).expect("write failed");
  file.write_all(leverage.as_bytes()).expect("write failed");

  let company = "company - Nestle\n";
  let shares = "shares - 8_000_000\n";
  let liabilities = "liabilities - 1_500_000\n";
  let date_founded = "date_founded - 1961\n";
  let leverage = "leverage - 81.3%\n";

  let mut file = std::fs::File::create("nestle.txt").expect("create failed");
  file.write_all(company.as_bytes()).expect("write failed");
  file.write_all(shares.as_bytes()).expect("write failed");
  file.write_all(liabilities.as_bytes()).expect("write failed");
  file.write_all(date_founded.as_bytes()).expect("write failed");
  file.write_all(leverage.as_bytes()).expect("write failed");

  let company = "company - Unilever\n";
  let shares = "shares - 37_000_000\n";
  let liabilities = "liabilities - 11_000_000\n";
  let date_founded = "date_founded - 1923\n";
  let leverage = "leverage - 70.3%\n";

  let mut file = std::fs::File::create("unilever.txt").expect("create failed");
  file.write_all(company.as_bytes()).expect("write failed");
  file.write_all(shares.as_bytes()).expect("write failed");
  file.write_all(liabilities.as_bytes()).expect("write failed");
  file.write_all(date_founded.as_bytes()).expect("write failed");
  file.write_all(leverage.as_bytes()).expect("write failed");

  let company = "company - Honeywell\n";
  let shares = "shares - 34_000_000\n";
  let liabilities = "liabilities - 9_000_000\n";
  let date_founded = "date_founded - 1906\n";
  let leverage = "leverage - 73.5%\n";

  let mut file = std::fs::File::create("Honeywell.txt").expect("create failed");
  file.write_all(company.as_bytes()).expect("write failed");
  file.write_all(shares.as_bytes()).expect("write failed");
  file.write_all(liabilities.as_bytes()).expect("write failed");
  file.write_all(date_founded.as_bytes()).expect("write failed");
  file.write_all(leverage.as_bytes()).expect("write failed");

  let company = "company - Nigerian Breweries\n";
  let shares = "shares - 30_000_000\n";
  let liabilities = "liabilities - 12_000_000\n";
  let date_founded = "date_founded - 1946\n";
  let leverage = "leverage - 60.0%\n";

  let mut file = std::fs::File::create("nigerian_breweries.txt").expect("create failed");
  file.write_all(company.as_bytes()).expect("write failed");
  file.write_all(shares.as_bytes()).expect("write failed");
  file.write_all(liabilities.as_bytes()).expect("write failed");
  file.write_all(date_founded.as_bytes()).expect("write failed");
  file.write_all(leverage.as_bytes()).expect("write failed");




















}




